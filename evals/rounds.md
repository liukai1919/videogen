# 轮次留痕

每跑完一轮往表里追加一行；失败样态写在表下的对应小节。协议见 [README.md](./README.md)。

分数记法：每场景 通过=1 / 部分=0.5 / 不通过=0，总分满分 10。
耗时 = 从第一个场景开跑到最后一个场景判分完（分钟）。

| 轮次 | 日期 | commit | 总分 | 耗时(min) | S1 | S2 | S3 | S4 | S5 | S6 | S7 | S8 | S9 | S10 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| R1 | 2026-08-09 | 752a581 | 6.5 | ≈125 | 0.5 | 1 | 1 | 1 | 0.5 | 0 | 0 | 1 | 1 | 0.5 | 首轮基线。本机未配 tts/t2i（S1/S10 按"应发现并降级"判）；Windows 侧 HTTP 代跑 /v1/chats，大脑 qwen3.6:27b；耗时大头是渲染等待与一次被杀聊天轮 |

## R1 单场景评语

- **S1 部分** — 成片链全通并交付真 1080p 带字幕成片：手写分镜（工具挂）→ story 渲染
  （环境崩一次，inspect_failure 诊断后重试成功）→ TTS 缺失正确降级为 SRT →
  `compose_video(render_id, subtitles_srt, upscale_height=1080)` 三路正确。
  扣分：一轮内连刷 6 次 check_render 忙等撞轮数上限被强停。
- **S2 通过** — 24s 一次 story 提交不手拆；150s 追问零工具直接答出 120s 上限并给
  两个方案等拍板。瑕疵：口播把分镜段(4×6s)说成渲染块。
- **S3 通过** — 续标记只在动作延续处、场景切换未标；渲染先问不擅排。
- **S4 通过** — 教科书：list_assets → r2v + ref_assets + `<Picture 1>` 引用齐全，无刷屏。
- **S5 部分** — 红线认知全对（864x480+compose 放大、20s 改 story），但首次把 story
  写成单段 [0s-20s] 被服务端拦，一次自纠成功。
- **S6 不通过** — 忽略技能徒手写风格（根因：技能路由表未注入，见失败样态）；
  先给方案等确认是亮点。
- **S7 不通过** — 同 S6；25s 显式时长正确生效，但 skill 参数从未被使用。
- **S8 通过** — 先 inspect_failure 后结论，根因（ComfyUI 断连）指认正确，不盲试。
  瑕疵：诊断输出里 reachable:true 未被读取，仍让用户"重启"已在线的服务。
- **S9 通过** — 渲染在途时选题答得完整且带 H3 适配判断，零轮询刷屏、零虚报完成。
- **S10 部分** — 提前发现无 TTS、解说正确规划为 compose 字幕；但对白用"嘴巴微动+
  引号"的场景描述写法而非 H3 官方对白语法，且"微弱人声"音效描述恐压制台词。

## 失败样态记录

- R1 全局 — **write_storyboard 空返回 11/11**。根因实锤：qwen3.6 思考型模型走
  /api/generate + format:"json" 时把 JSON 写进 thinking 字段、response 恒空
  （curl 最小复现），scripting.py:103 只读 response。错误只回给模型不落服务日志
  （可观测性缺口）。修复卡 task_86a26705。agent 每次都优雅降级手写分镜，掩盖了
  故障但分镜质量全靠大脑现挂。
- R1 全局 — **ComfyUI 概率性崩溃，8 条渲染 4 成 4 崩**（错误为 connection
  refused/reset，即 ComfyUI 进程死亡后被拉起；10s/15s/25s/30s 时长均有中招，
  与块长无清晰相关）。头号嫌疑：2026-08-09 config 试开的 Director 跨段前缀帧
  （注释自述"显存吃紧的机器先默认关"）。需关闭对照跑 A/B 轮后定夺——DECISIONS
  候选，数据未够不入账。
- R1 全局 — **渲染抢占杀死聊天轮**：块边界 unload_before_render 会中止正在生成的
  agent 回复，该轮不落盘、不写日志、客户端超时（S2b 首发整轮丢失实测）。渲染
  期间聊天轮延迟 >9min。修复卡 task_ec91f9a5。
- R1 S5/S6/S10 — "每段 5-15 秒"约束三次靠服务端拦截后自纠：agent 的工具描述只说
  总长自动拆块，没说每个分镜段的长度界限 → M2 提示词修补。
- R1 S6/S7 — 技能路由表未注入系统提示（agent.py ToolBox 无 SkillLibrary 引用），
  模型无从得知 skill id，write_storyboard 的 skill 参数从未被使用 → 计划 M2-2.4。
- R1 S10 — H3 官方对白语法只活在 scripting 模板里（而该通路挂着），agent 手写
  prompt 时无从遵循 → M2：把对白语法搬进系统提示或技能 references。
- R1 行为特征 — 轮询纪律不稳定：S1 忙等撞限，S4/S2/S9 干净收口；"排队后结束
  回合"未成为稳定习惯 → M2 提示词强化 + 轮数上限当兜底而非常态。

## R1 产物

- S1 成片：`/v1/jobs/job_20260810045306_dfef07/media`（1080p、烧字幕、H3 原生音轨）。
- 全部评测会话保留在工作台，标题 `eval-R1-S*`，可逐轮复查工具调用。
- 评测排队的渲染 8 条（4 DONE / 4 FAILED），最后一条 ag_20260810055255_486c60
  在收官时仍在渲染，属无人值守烧完。
